export const createSalesforceRecord = async (application) => {
    console.log(`💼 Creating Salesforce record for ${application.productType} with applicant ${application.fullName}`);
    // Later, call Salesforce REST API here
    return true;
};
